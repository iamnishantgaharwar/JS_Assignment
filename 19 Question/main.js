const countries = require("./countries");
const webTechs = require("./web_tech");

console.log(countries); // prints the array of countries
console.log(webTechs); // prints the array of web technologies